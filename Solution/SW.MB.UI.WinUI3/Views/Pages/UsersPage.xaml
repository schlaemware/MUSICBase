<Page
    x:Class="SW.MB.UI.WinUI3.Views.Pages.UsersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SW.MB.UI.WinUI3.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:observables="using:SW.MB.UI.WinUI3.Models.Observables"
    mc:Ignorable="d">

  <Page.Resources>
    <DataTemplate x:Key="UserListItemTemplate" x:DataType="observables:ObservableUser">
      <Grid Margin="0,12">
        <TextBlock VerticalAlignment="Center"
                   Text="{Binding Fullname}"
                   Style="{ThemeResource BodyStrongTextBlockStyle}"/>
      </Grid>
    </DataTemplate>

    <DataTemplate x:Key="UserDetailItemTemplate" x:DataType="observables:ObservableUser">
      <ScrollViewer Margin="20,-0,0,0"
                    VerticalScrollMode="Enabled"
                    HorizontalScrollMode="Disabled">
        <StackPanel HorizontalAlignment="Left"
                    Orientation="Vertical">
          <TextBlock VerticalAlignment="Center"
                     Text="{Binding Fullname}"
                     Style="{ThemeResource HeaderTextBlockStyle}"
                     Margin="0,0,0,20" />
        </StackPanel>
      </ScrollViewer>
    </DataTemplate>
  </Page.Resources>

  <Grid>
    <controls:ListDetailsView ItemsSource="{x:Bind ViewModel.UsersCollection}" 
                              ItemTemplate="{StaticResource UserListItemTemplate}"
                              DetailsTemplate="{StaticResource UserDetailItemTemplate}"
                              SelectedItem="{x:Bind ViewModel.SelectedUser}"
                              Background="Transparent" />
  </Grid>
</Page>
