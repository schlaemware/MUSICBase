<Page
    x:Class="SW.MB.UI.WinUI3.Views.Pages.MembersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SW.MB.UI.WinUI3.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:SW.MB.UI.WinUI3.Converters"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:observables="using:SW.MB.UI.WinUI3.Models.Observables"
    mc:Ignorable="d">

  <Page.Resources>
    <converters:BooleanInvertingConverter x:Key="BooleanInverter" />
    <converters:BooleanToVisibilityConverter x:Key="BoolToVisConverter" />

    <DataTemplate x:Key="MembersListItemTemplate" x:DataType="observables:ObservableMember">
      <Grid Padding="0,12">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"
                   VerticalAlignment="Center"
                   Text="{Binding Fullname}"
                   Style="{ThemeResource BodyStrongTextBlockStyle}" />
        <TextBlock Grid.Row="1"
                   VerticalAlignment="Center"
                   Text="{Binding Instrument}"
                   Style="{ThemeResource BodyTextBlockStyle}" />
      </Grid>
    </DataTemplate>

    <DataTemplate x:Key="MembersDetailItemTemplate" x:DataType="observables:ObservableMember">
      <ScrollViewer Margin="20,-0,0,0"
                    VerticalScrollMode="Enabled"
                    HorizontalScrollMode="Disabled">
        <StackPanel HorizontalAlignment="Left"
                    Orientation="Vertical">
          <StackPanel.Resources>
            <Style TargetType="Grid">
              <Setter Property="Height" Value="40" />
              <Setter Property="HorizontalAlignment" Value="Left" />
            </Style>
          </StackPanel.Resources>
          <TextBlock VerticalAlignment="Center"
                     Text="{Binding Fullname}"
                     Style="{ThemeResource HeaderTextBlockStyle}"
                     Margin="0,0,0,20" />

          <!-- Firstname -->
          <Grid Visibility="{Binding ElementName=EditButton, Path=IsChecked, Converter={StaticResource BoolToVisConverter}}">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="100" />
              <ColumnDefinition Width="40" />
              <ColumnDefinition Width="*" MinWidth="200" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       Text="Firstname"
                       Style="{ThemeResource BodyStrongTextBlockStyle}"
                       VerticalAlignment="Center" />
            <TextBox Grid.Column="2"
                     Margin="-12,0,0,0"
                     Text="{Binding Firstname}"
                     VerticalAlignment="Center"
                     IsReadOnly="{Binding ElementName=EditButton, Path=IsChecked, Converter={StaticResource BooleanInverter}}" />
          </Grid>

          <!-- Lastname -->
          <Grid Visibility="{Binding ElementName=EditButton, Path=IsChecked, Converter={StaticResource BoolToVisConverter}}">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="100" />
              <ColumnDefinition Width="40" />
              <ColumnDefinition Width="*" MinWidth="200" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       Text="Lastname"
                       Style="{ThemeResource BodyStrongTextBlockStyle}"
                       VerticalAlignment="Center" />
            <TextBox Grid.Column="2"
                     Margin="-12,0,0,0"
                     Text="{Binding Lastname}"
                     VerticalAlignment="Center"
                     IsReadOnly="{Binding ElementName=EditButton, Path=IsChecked, Converter={StaticResource BooleanInverter}}" />
          </Grid>
          
          <!-- Instrument -->
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="100" />
              <ColumnDefinition Width="40" />
              <ColumnDefinition Width="*" MinWidth="200" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       Text="Instrument"
                       Style="{ThemeResource BodyStrongTextBlockStyle}"
                       VerticalAlignment="Center" />
            <TextBlock Grid.Column="2"
                       Text="{Binding Instrument}"
                       Style="{ThemeResource BodyTextBlockStyle}"
                       VerticalAlignment="Center"
                       Visibility="{Binding ElementName=EditButton, Path=IsChecked, Converter={StaticResource BoolToVisConverter}, ConverterParameter='true'}" />
            <ComboBox Grid.Column="2"
                      VerticalAlignment="Center"
                      HorizontalAlignment="Stretch"
                      SelectedIndex="0"
                      IsEditable="False"
                      Margin="-12,0,0,0"
                      Visibility="{Binding ElementName=EditButton, Path=IsChecked, Converter={StaticResource BoolToVisConverter}}">
              <ComboBoxItem Content="Querflöte" />
              <ComboBoxItem Content="Klarinette" />
              <ComboBoxItem Content="Trompete" />
            </ComboBox>
          </Grid>
        </StackPanel>
      </ScrollViewer>
    </DataTemplate>
  </Page.Resources>
  
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="Auto" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <CommandBar Grid.Row="0" Grid.Column="0"
                Background="Transparent">
      <AppBarButton Icon="Add" Label="Add" />
      <AppBarSeparator />
      <AppBarToggleButton x:Name="EditButton"
                          Icon="Edit"
                          Label="Edit" />
      <AppBarSeparator />
    </CommandBar>

    <controls:ListDetailsView Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                              ItemsSource="{x:Bind ViewModel.MembersCollection}"
                              ItemTemplate="{StaticResource MembersListItemTemplate}"
                              DetailsTemplate="{StaticResource MembersDetailItemTemplate}"
                              SelectedItem="{x:Bind ViewModel.SelectedMember, Mode=TwoWay}"
                              Background="Transparent" />
  </Grid>
</Page>
