<Page
    x:Class="SW.MB.UI.WinUI3.Views.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SW.MB.UI.WinUI3.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:xaml="using:Microsoft.UI.Xaml"
    xmlns:converters="using:SW.MB.UI.WinUI3.Converters"
    xmlns:enumerations="using:SW.MB.UI.WinUI3.Models.Enumerations"
    mc:Ignorable="d">

  <Page.Resources>
    <converters:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />
  </Page.Resources>

  <Grid>
    <StackPanel x:Name="ContentArea">
      <TextBlock x:Uid="Settings_Personalization" Style="{ThemeResource SubtitleTextBlockStyle}" />

      <StackPanel Margin="{StaticResource SmallTopBottomMargin}">
        <TextBlock x:Uid="Settings_PersonsDisplayMode" Style="{ThemeResource BodyStrongTextBlockStyle}" />
        <StackPanel Margin="{StaticResource XSmallTopMargin}">
          <StackPanel.Resources>
            <Style TargetType="RadioButton">
              <Setter Property="Command" Value="{x:Bind ViewModel.SwitchPersonsDisplayCommand}" />
              <Setter Property="GroupName" Value="PersonsDisplayModeGroup" />
            </Style>
          </StackPanel.Resources>
          
          <RadioButton x:Uid="Settings_PersonsDisplayMode_FirstnameLastname"
                       IsChecked="{x:Bind ViewModel.PersonsDisplayMode, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=FirstnameLastname, Mode=OneWay}"
                       CommandParameter="{x:Bind enumerations:PersonsDisplayMode.FirstnameLastname}" />
          <RadioButton x:Uid="Settings_PersonsDisplayMode_LastnameFirstname"
                       IsChecked="{x:Bind ViewModel.PersonsDisplayMode, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=LastnameFirstname, Mode=OneWay}"
                       CommandParameter="{x:Bind enumerations:PersonsDisplayMode.LastnameFirstname}" />
        </StackPanel>
      </StackPanel>

      <StackPanel Margin="{StaticResource SmallTopBottomMargin}">
        <TextBlock x:Uid="Settings_PersonsOrderingMode" Style="{ThemeResource BodyStrongTextBlockStyle}" />
        <StackPanel Margin="{StaticResource XSmallTopMargin}">
          <StackPanel.Resources>
            <Style TargetType="RadioButton">
              <Setter Property="Command" Value="{x:Bind ViewModel.SwitchPersonsOrderingCommand}" />
              <Setter Property="GroupName" Value="PersonsOrderingModeGroup" />
            </Style>
          </StackPanel.Resources>
          
          <RadioButton x:Uid="Settings_PersonsOrderingMode_LastnameFirstnameBirthdate"
                       IsChecked="{x:Bind ViewModel.PersonsOrderingMode, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=LastnameFirstnameBirthdate, Mode=OneWay}"
                       CommandParameter="{x:Bind enumerations:PersonsOrderingMode.LastnameFirstnameBirthdate}" />
          <RadioButton x:Uid="Settings_PersonsOrderingMode_FirstnameLastnameBirthdate"
                       IsChecked="{x:Bind ViewModel.PersonsOrderingMode, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=FirstnameLastnameBirthdate, Mode=OneWay}"
                       CommandParameter="{x:Bind enumerations:PersonsOrderingMode.FirstnameLastnameBirthdate}" />
        </StackPanel>
      </StackPanel>

      <StackPanel Margin="{StaticResource SmallTopBottomMargin}">
        <TextBlock x:Uid="Settings_Theme" Style="{ThemeResource BodyStrongTextBlockStyle}" />
        <StackPanel Margin="{StaticResource XSmallTopMargin}">
          <StackPanel.Resources>
            <Style TargetType="RadioButton">
              <Setter Property="Command" Value="{x:Bind ViewModel.SwitchThemeCommand}" />
              <Setter Property="GroupName" Value="AppThemeGroup" />
            </Style>
          </StackPanel.Resources>
          
          <RadioButton x:Uid="Settings_Theme_Light"
                       IsChecked="{x:Bind ViewModel.Theme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Light, Mode=OneWay}"
                       CommandParameter="{x:Bind xaml:ElementTheme.Light}" />
          <RadioButton x:Uid="Settings_Theme_Dark"
                       IsChecked="{x:Bind ViewModel.Theme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Dark, Mode=OneWay}"
                       CommandParameter="{x:Bind xaml:ElementTheme.Dark}" />
          <RadioButton x:Uid="Settings_Theme_Default"
                       IsChecked="{x:Bind ViewModel.Theme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Default, Mode=OneWay}"
                       CommandParameter="{x:Bind xaml:ElementTheme.Default}" />
        </StackPanel>
      </StackPanel>

      <TextBlock x:Uid="Settings_About" Style="{ThemeResource SubtitleTextBlockStyle}" />

      <StackPanel Margin="{StaticResource XSmallTopMargin}">
        <TextBlock Text="{x:Bind ViewModel.VersionString, Mode=OneWay}"
                   Margin="{StaticResource XSmallTopMargin}"
                   Style="{ThemeResource BodyTextBlockStyle}" />
        
        <TextBlock Text="{x:Bind ViewModel.VersionDescription, Mode=OneWay}"
                   Margin="{StaticResource XSmallTopMargin}"
                   Style="{ThemeResource BodyTextBlockStyle}" />

        <TextBlock x:Uid="Settings_AboutDescription"
                   Margin="{StaticResource XSmallTopMargin}"
                   Style="{ThemeResource BodyTextBlockStyle}" />

        <HyperlinkButton x:Uid="SettingsPage_PrivacyTermsLink"
                         Margin="{StaticResource SettingsPageHyperlinkButtonMargin}" />
      </StackPanel>
    </StackPanel>
  </Grid>
</Page>
