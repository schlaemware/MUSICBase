<UserControl x:Class="SW.MB.UI.WPF.Views.Controls.UpdatesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SW.MB.UI.WPF.Views.Controls"
             xmlns:vms="clr-namespace:SW.MB.UI.WPF.ViewModels"
             xmlns:selectors="clr-namespace:SW.MB.UI.WPF.Models.TemplateSelectors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vms:UpdatesViewModel, IsDesignTimeCreatable=False}">

  <UserControl.Resources>
    <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
    <selectors:ReleaseTemplateSelector x:Key="ReleaseTemplateSelector" />
  </UserControl.Resources>

  <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled">
    <StackPanel Orientation="Vertical">
      <ItemsControl ItemsSource="{Binding ReleasesView}" ItemTemplateSelector="{StaticResource ReleaseTemplateSelector}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <StackPanel Orientation="Vertical" />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
      </ItemsControl>
    </StackPanel>
  </ScrollViewer>
</UserControl>
